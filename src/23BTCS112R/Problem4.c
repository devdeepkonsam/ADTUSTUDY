#include <stdio.h>
int main()

/*C Program to Print a Float (Entered by the User)*/

{
    float num;
    printf("Enter the number: ");
    scanf("%f",&num);
    printf("%.2f",num);// print only 2 decimal point
    return 0;

}
